# Code review 2026-09-25 — merged analysis and routing (architect)

Two read-only reviewers on solex `9b30ff9` (vi complete, 517 unit green). A = quality/organization/clarity (`2026-09-25-quality.md`), B = correctness (`2026-09-25-correctness.md`, every finding reproduced in a scratch copy or traced). This file is the verdict and the routing; the reports hold the detail.

## Verdict

- **Architecture is real** (A: grade B+). One `Hotel` object whose methods are the commands, one write path with retry + idempotency, lint-enforced store boundary, 517 fast tests on real SQLite that read like the spec, structural PII discipline. Weakness is copy-paste where helpers should exist (ledger write ×3, account opening as a nested commit ×13), read models stuck in adapters, UI checking role names, and comment rot.
- **Ledger core holds** (B): entries balance, balances are sums not stored, replay reproduces state, commandId dedupe and the night roll are race-safe, every write is capability-checked server-side, tenancy scoped. **The bugs are at the edges of a stay** and four of them sit on the demo path: early check-out, reprice of a past night, check-out in credit, moving an in-house guest.
- **Nothing in A blocks the demo. Four things in B do.**

## Rulings (architect, D-21)

| # | ruling |
|---|---|
| B1 early check-out | Nights are `[arrival, departure)` (D-7 wins over §10 6a wording). Check-out frees the current business date's night and reverses its room charge in the same batch. Product fixes §10 6a. Late check-out fee stays a catalogue item. |
| B2 reprice past night | The reposted line keeps the original line's `businessDate`; the reversal is dated today. Applies to every category, not only `room`. |
| B3 check-out in credit | Refuse `stay.folioInCredit`; the settle dialog offers the refund. Account closes only at 0 (no bypass). |
| B4 move in-house | `fromDate` defaults to today for checked-in stays; refuse if before today or any affected night is posted; refuse out-of-order target; check room exists; old room marked dirty. |
| B8 void in-house room night | Refuse with a code that points to reprice (the roll would re-post it anyway). Demo script: don't void a room line. |
| B14 refund after forfeit | Cap the refund at deposits − refunds − forfeits (mirror of the forfeit cap). Amends mvp.md §3 refund bullet. |
| B15 default roll | Default `rollHour` 02:00 per D-7; mvp.md §3 was right, the code default was not. |
| A8 layout / naming | No restructure before the demo. "account" rename to `users`/`logins` after. |

## Routing

**Wave 1 — before the demo (dev, now):** B1, B2, B3, B4, B8. Each with the scenario test from B §4. Then QA re-runs the suite and re-films R1 if the check-out beat changed.

**Wave 2 — cheap correctness, dev idle anyway (after wave 1):** B5 read-side role gate, B6 operator/foreign-account guard, B7 version availability on group create/add, B9 transfer cap + company check, B10 approval carries method + rule checks at request, B12 cancel group with terminal stays, B13 first staff must be an owner, B14, B15 default roll hour + lazy-account race (`openAccount` no-op when exists), A3 `grant` decides inside the plan.

**Follow-ups (mvp.md §4, not now):** B11 commandId scoping (ULID + reserved prefixes + hotel scope), B15 rest (rebuild atomicity, OOO demand undercount, unbounded span, last-owner race, move/reprice revenue dates, expense backdating, notes PII in the log, same-id refusal), A1+A2 ledger write helper + accounts in batch (M), A4 read models on `Hotel` (M), A5 `useCan` (S), A6 split `setup.tsx` (M), A7 typed events (L), A8 rename (S), A9 comment sweep (S), A10 drop roomType shims (S), A11–A15 (S/M).

**QA:** B §4 test gaps → cases where screen-visible; the rest are dev unit scenarios. Add "void a room line of an in-house guest" to the do-not-demo list in the journey docs.
