# Catch-up

**Doing:** read roster, profile, project README, requirements (+ raw), team/*, workflow.md, secrets shape (values not read into notes).

**Found:**
- Raw client text literally says "DASHBOARD EXCEL" + "Sheet :" → workbook is the intended/current shape. Q stands: does it exist, can we get it.
- Access file: public + LAN URL, both plain `http://` on `:99`, one user/pass. Cleartext creds over public internet — don't log in from untrusted networks; flag to Alex.
- LAN URL exists → system likely hosted on-prem at hotel (relevant to WS1: LAN/offline expectations).
- Tooling: curl 8.7 ✓, node 22 + npx ✓, Playwright chromium binaries cached (`~/Library/Caches/ms-playwright`) but no `playwright` pkg installed locally (npx would fetch) and **no Playwright MCP** in session. jq ✓; no htmlq/pup.

**Blocked:** on Alex — what `:99` is, workbook, walkthrough format (live drive vs screenshots/exports).

**Next:** first Qs to Alex: (1) what is `:99` (PMS vendor? who set it up? staff still use it?); (2) Excel workbook exists?; (3) OK for me to hit it with curl/headless browser, read-only, or Alex-driven walkthrough?; (4) Vietnamese UI — any VN-speaking staff for walkthrough?
