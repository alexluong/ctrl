# 2026-09-24 01:00 — e2e suite up

- Worktree: took over `~/git/hub/alexluong/solex-qa` from architect (Alex: name = agent). Port base 7020; e2e server on 7021, `data/e2e.db` rebuilt per run.
- solex `ef6d242`: `e2e/` own package + pnpm root (no root lockfile change). `scripts/fresh-db.sh` puts real node first on PATH — mise shims re-apply `.env` and silently override exported `DATABASE_URL` (N13).
- Specs assert via `src/i18n/messages.ts` `vi` dictionary, not literal strings.
- `open()` waits for networkidle: pre-hydration click = native GET with fields in URL (N11).
- Projects: `setup` (empty-hotel check, then owner setup: room type, rate today-7..+120, standard categories) → `desk`.
- Known-red in setup project marked `test.fail` (N10 empty select) so it doesn't skip desk specs; delete when fixed.
- Pending: ask dev to add `"e2e"` to root tsconfig exclude (fresh clone without e2e install → root tsc can't resolve @playwright/test). Receivables (landing 6, 1ad54f4 unpushed) next.
