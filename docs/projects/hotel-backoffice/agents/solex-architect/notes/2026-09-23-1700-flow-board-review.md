# flow board review

**Doing:** reviewed explore's Excalidraw flow spec (`tools/excalidraw/flow.json`) + existing-system.md §group flow / individual flow / charges.

**Verdict:** capture side (booking two paths, charge shape, config checklist) is complete and consistent with product v0.1. **Lifecycle + money side is thin** — exactly what ES needs: settlement act, receivable create/settle, group post-booking lifecycle (assignment, rooming list, check-in granularity), master folio, deposits, cancellation, nightly posting vs checkout, date semantics of overbooking, room status transitions, mid-stay changes. Sent 10 numbered Qs to explore (msg cfdc7752); priority 1, 2, 7, 8.

**Also:** board's "WHAT MATTERS" panel contradicts itself (two vs three personas) — flagged.

**Next:** when explore answers, merge to README; then hand product the full story for v1. Product stays paused per Alex until exploration feels complete.
