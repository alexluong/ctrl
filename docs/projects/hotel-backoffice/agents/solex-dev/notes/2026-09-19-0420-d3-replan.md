# D-3 replan (TS Workers + D1)

**Doing:** re-read objective rev 2 + D-3. Go dropped; Containers/paid Qs from previous entry moot. Still not started — waiting on Alex + `wrangler login`.

**Plan sketch (for Alex to poke at):**
- Single Worker, **Hono** router (tiny, Workers-first, typed bindings, `hono/client` gives typed RPC to a React UI later). Alternative "none" = raw `fetch` handler; fine for hello-world but we'd reinvent routing/middleware day 2.
- **Workers Static Assets** (not Pages) — one deploy unit, API + UI same origin, no CORS. Pages is effectively being folded into Workers by CF.
- UI plumbing: Vite + `@cloudflare/vite-plugin` so `vite dev` runs the Worker in workerd with D1 locally + HMR for the UI. Plain HTML page hitting `/api/...` is enough to prove it; React choice deferred.
- D1: `wrangler d1 migrations` (plain SQL files). Local = miniflare SQLite, remote = `--remote`.
- `fix` / `check`: Biome (format+lint, one tool) + `tsc --noEmit`; `vitest` w/ `@cloudflare/vitest-pool-workers` for tests. Scripts in package.json (pnpm).

**Questions for Alex:**
1. Hono ok? (lean yes)
2. Vite plugin dev loop vs bare `wrangler dev`? Lean Vite — whichever UI framework wins needs it anyway.
3. Biome vs ESLint+Prettier? Lean Biome (one tool, fast).
4. Package manager: pnpm? Check what collie-ui uses — align.
5. Deploy target: `*.workers.dev` for spike? Lean yes.
6. D1 for ES — honest early take to verify in spike: fine at single-hotel scale (append table w/ `(stream_id, version)` UNIQUE = optimistic concurrency; D1 single-writer serializes anyway). Limits: 10GB/db, no LISTEN/NOTIFY (projections via Queues or inline), transactions only via `batch()` (no interactive tx). DO-per-aggregate = stronger ordering but over-engineered here. Flag only, not build.

**Blocked:** `wrangler login` (Alex), Alex scope talk.

**Next:** Alex engages → `/new-project solex` → spike.
