# Caught up — awaiting scope discussion with Alex

**Doing:** session start. Read profile, roster, README, requirements, team/*, workflow. No `discovery.md` / `existing-system.md` yet.

**Found / first read of the domain (pre-interview, unvalidated):**
- Client model is a spreadsheet: DASHBOARD + 7 sheets. Everything derives from Booking + Payment rows; "Need" cascade in §1 = one event fanning to projections. ES fit is real but the *write* side is small: Booking lifecycle + money.
- Candidate aggregates: **Booking** (core; owns stay dates, room assignment, guests, deposit, status), **Room** (status board — housekeeping state is per-room, not per-booking; "Expected arrival/departure" are *derived* from bookings, "Dirty/Clean/OOO" are *housekeeping facts* — the client conflates them into one status list; must split), **Folio** (charges + payments per booking; revenue line items §4.4, payment methods §4.5), **Receivable** (B2B/OTA/group debt — may just be a Folio projection filtered by debtor type + due date, not its own aggregate), **Expense** (separate context, cash-out ledger), **Guest** (probably a lookup/projection keyed by phone/CCCD, not an aggregate), master data (RoomType, Floor, Channel w/ commission %, RateTable).
- Two booking IDs (internal + reservation no) → OTA ref is external ref on Booking, internal is ours.
- Invariants to confirm: no room double-assignment over overlapping [checkin, checkout); nights = checkout − checkin in hotel-local dates (no tz on dates at all — store as dates, not timestamps; hotel tz only for "today"); balance = charges − payments − deposit; commission = rate × room revenue by channel.
- Gaps to interview on: OTA commission rates + who pays (OTA collects vs hotel collects), seasonal rates / per-night rate override, VAT / red invoice, channel manager vs manual, group bookings (one booking many rooms?), corporate accounts, refunds/cancellation policy, currency (VND only?), who the users actually are.

**Blocked:** interview w/ Alex (README open questions + gaps above). Architect says wait.

**Next:** once Alex shows up → interview, then draft `product.md` skeleton + message architect w/ aggregate list early per objective.
